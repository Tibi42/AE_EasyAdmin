// Démarre l'application Stimulus avec le bundle Symfony
// Enregistre automatiquement tous les contrôleurs définis dans assets/controllers/
import { startStimulusApp } from '@symfony/stimulus-bundle';

const app = startStimulusApp();
